# Round 24.10: arcane lv3 at 6 g + the spell-before-refund rule (2026-08-14)

Remi's rulings after the C6/E1 discussion: C6-bolt-combo stays untouched
(proof of the skill ceiling: K2-permafrost, the same package piloted, was #1
on the 24.9 Hard table); arcane lv3 drops 10 -> 6 g (flat like lv1/2, costs
[6,6,6]); and no strategy buys arcane 3 until it owns at least one kit spell
for the lv3 refund to shave.

## What changed

- `shared/constants.js`: ELEMENTS.arcane.costs [6,6,10] -> [6,6,6] (revert
  path in the comment). Question M stands measured: CDR is fairly priced at
  best; the lv3 premium was pricing a special that is dead weight without a
  kit.
- `tools/roster.js`: lightning 1 now precedes arcane 3 in B1, D4, E1, E2,
  F1+F2 (identical edit both sides, the A/B holds) and the shared M scaffold
  (all six M rows stay byte-identical, 172 g). A3-elements-only keeps its
  zero-spell purity by stopping at arcane 2 in the core; the exhaust tail
  buys lightning (4th) before arcane (12th), so every below-band core is
  compliant by construction. In-game BUILDS already complied (stormcaller
  buys lightning before its third arcane).
- Guard: test/report.test.js walks every padded core and fails any future
  build that reaches arcane 3 spell-less; also locks the tail order. 529
  vitest green.

## Measured (both standard tables rerun, 2565 g seed 1; pages
`...elo-2565g-seed1-3.html` and `...elo-2565g-seed1-faker-3.html`)

A deliberately small patch and it reads small; vs the 24.9 tables (±40 =
noise, D1 anchor 1598/1611):

- The reordered rows drifted UP a notch on Hard: D4-leech 1617 -> 1654 (best
  non-K/G row again), F2 1276 -> 1331, B1 1524 -> 1561; E1/E2 flat on Hard
  (the discount does not fix a glass core).
- On Faker the one real move is **E1-hastemaker 1233 -> 1321 (+88)**: with
  lightning owned before arcane 3, the refund engine finally runs inside the
  145 g a game actually pays.
- Nothing destabilized: K family 2463-2763 on Hard, top-9 on Faker still
  diverse (B2/A3/M rows), floor still A4 (1044/970) and B4 (1012/909).
- A3-elements-only is now an 88 g core (arcane 3 left it) and sits rank 2 on
  the Faker table: the cheapest strong build in the game, a good "new player
  buys only elements" sanity fact.

Raw tables live in the two HTML pages beside this file (hover any row for
the build); numbers quoted above are from the verbatim tool output.
